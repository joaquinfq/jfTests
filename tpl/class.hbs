{{#super}}
const {{super}} = require('{{{insuper}}}');
{{/super}}
const {{name}} = require('{{{infile}}}');
const jfTestsUnit = require('@jf/tests/src/type/Unit');

/**
 * Pruebas unitarias de la clase `{{title}}`.
 */
module.exports = class {{name}}Test extends jfTestsUnit
{
    /**
     * @override
     */
    static get title()
    {
        return '{{title}}';
    }

    /**
     * @override
     */
    setUp()
    {
        this.sut = new {{name}}();
    }

    /**
     * Comprueba la definición de la clase.
     */
    testDefinition()
    {
        this._testDefinition(
            {{name}},
            {
                // Propiedades estáticas
            },
            {
                // Propiedades de instancia.
            }
        );
    }
{{#super}}

    /**
     * Comprueba la herencia de la clase.
     */
    testInheritance()
    {
        this._testInheritance({{name}}, {{super}}, null);
    }
{{/super}}
{{#methods}}

    /**
     * Pruebas del método{{#static}} estático{{/static}} `{{method}}`.
     */
    {{test}}()
    {
{{^static}}
        const _sut = this.sut;
{{/static}}
    }
{{/methods}}
};
